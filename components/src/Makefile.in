DEPTH                 = ../../..
topsrcdir             = @top_srcdir@
srcdir                = @srcdir@
VPATH                 = @srcdir@

include $(DEPTH)/config/autoconf.mk

IS_COMPONENT          = 1
MODULE                = findbar
LIBRARY_NAME          = fbTextExtractor

XPI_NAME              = findbar

REQUIRES = xpcom \
           string \
           dom \
           $(NULL)

CPPSRCS = fbTextExtractor.cpp \
          FindBarModule.cpp \
          $(NULL)

include $(topsrcdir)/config/rules.mk

EXTRA_DSO_LDOPTS += \
  $(XPCOM_GLUE_LDOPTS) \
  $(NSPR_LIBS) \
  $(NULL)
